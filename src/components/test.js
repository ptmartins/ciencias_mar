// Create a vanilla Javascript function that sanitizes an html string. The function should remove all html tags from the string and return the sanitized string. The function should be named sanitizeHtml and should take a single argument, htmlString. The function should be exported as a default export.

// Example usage:
// import sanitizeHtml from './test';
// const htmlString = '<p>hello world</p>';
// const sanitizedString = sanitizeHtml(htmlString);



